# SOPS configuration for rubi
# Uses the same admin age key as cobalto for secret encryption

keys:
  - &admin age1ka44gzmhvrgaxn5pm9eml0k4srh5rjxr87dtrtn4exmwcxz2yeusjrmdne

creation_rules:
  - path_regex: secrets\.yaml$
    key_groups:
    - age:
      - *admin
